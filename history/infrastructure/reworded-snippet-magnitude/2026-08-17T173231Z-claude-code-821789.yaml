history_version: 1
target:
  kind: infrastructure
  path: scripts/audit_evidence_snippets.py
  slug: reworded-snippet-magnitude
session:
  id: 2026-08-17T173231Z-claude-code-821789
  timestamp: '2026-08-17T17:32:31Z'
  actors:
  - type: ai_agent
    name: claude-code
links:
  issues:
  - https://github.com/CultureBotAI/TraitMech/issues/292
events:
- type: EDIT
  outcome: changed
  sections:
  - evidence
  summary: Let a reworded shared snippet compare against the magnitude it could only be
  details: '292 recorded the cost of 291''s magnitude key: it folds the snippet TEXT in, so
    editing a shared snippet - rewording it, not diversifying it - produced an unseen key,
    its baselined magnitude read as 0, any count beat it, and a curator was told something
    got worse when nothing had. The remedy they reach for is --write-baseline, which is the
    rot 270 was about. 292 rejected the obvious repair, falling back to the graph''s per-key
    MAX, and it was right to: with two baselined groups of 4 and 2, the 2 could be reworded
    and grown to 4 while the max sheltered it, restoring exactly what 291 removed. THAT OBJECTION
    NEEDS TWO GROUPS. When a graph has exactly ONE baselined magnitude under this key there
    is nothing smaller to grow into something larger, so comparing a reworded snippet against
    the only magnitude it could be is safe by construction rather than by luck. Measured before
    writing it: 13 REUSED_SNIPPET rows across 12 graphs, and one graph - trophic_type, the
    case 291 was written about - carries two. So the fallback covers eleven of twelve and
    leaves the twelfth failing closed exactly as today. Probed on real data rather than trusting
    the reasoning. Rewording the shared snippet in animal_pathogen.yaml, a single-group graph,
    previously read as new and now yields zero new findings. Rewording carbon source in trophic_type.yaml,
    the two-group graph, still yields one new blocking finding, so the sheltering 292 warned
    about cannot occur. Four tests pin both halves plus two adjacent traps: that the fallback
    still ratchets when a reworded group also GROWS, and that it is scoped by _key so a different
    graph''s magnitude cannot shelter this one. 569 tests pass, qc green.'
